- [ ] ハンドが右ハンド固定
	- [ ] ArenaPlaceにHandSideを追加すべきかも
- [ ] 経由地点とか目標点を何も設定してない
- [ ] 

メモ

合体コマンドを押してからじゃないと上段配置ボタンを押せない

下段
中段
合体

R2がbackにいる状態で中段配置した後はR2はfront用の位置にいどうする


Arenaで使うシーケンスを作る
入力は0-8の整数で3x3の9マスのうちどれに入れるか
前までkfs_placeは3x3の9マスのうち下段のみ配置するものだったが今回はそれを改良、もしくはkfs_placeをシーケンスの一部としてラップする
R2の待機位置はfrontとbackの2箇所
R2の位置は前回に何の司令を送ったかと、どのエリアでシーケンスをスタートしたかで判断する

下段配置
kfs_placeの基本配置(省略じゃない方)のときの経路生成に経由点を追加する
R2に待機位置がbackとfrontの2箇所あってそれによって経由地点が異なる
(経由地点を通る方法は後ほどcomputetrajectoryに追加する感じで作られる。まだ未実装)

中段配置
R2にArucoコマンドを見せて中段配置をR2にさせる
ArucoコマンドをR1がR2に見せる場所はR2がbackかfrontにいるかによってかわる
また、R1が前回の動きで下段配置をしたときはR1のほうがR2よりもTTTラックに近いような位置関係になっているのでR2がbackにいるときのArucoコマンドを見せる場所にR2を避けるように経由地点を設定して移動してコマンドを見せて中段配置させる
まだ実装されてないけど
```
asl_ros_interfaces/BaseState start
asl_ros_interfaces/BaseState goal

# start と goal の間に通りたい点(twistは現在使わない)
asl_ros_interfaces/BaseState[] waypoints

uint8 ROUTE_AUTO=0
uint8 ROUTE_CW=1
uint8 ROUTE_CCW=2
uint8 route_direction

# 0以下の場合はwaypoint付近のoffset制限を使わない
float64 waypoint_tolerance
float64 waypoint_bound_radius
---
asl_ros_interfaces/Trajectory trajectory
bool success
string message
```
ComputeTrajectory.srvはこうなる予定

上段配置
コマンドを見せるところまでは中段配置と同じ
上段配置は合体して行う
コマンドを見せたら合体シーケンスが始まって合体した後、配置位置に移動して配置
中段と共有のところがあれば共有関数にしてもいいかも


## 入力

**TTTのどこに入れるか**

```c++
enum class R2ArenaPosState {
	FRONT,
	BACK,
	NONE
}

```



## 状態

何の動作をした後かで位置をよ臆して条件分岐



## 動き

### R1
#### R2が後ろ待機位置にいてR1が下段配置するとき 
下段配置
経由地2点
![[Pasted image 20260703001628.png]]

#### R2が後ろにいてR1がR2に指示を出しに行くとき
合体or中段配置
![[Pasted image 20260703002106.png]]

#### R2が前待機してて後ろに下げるとき
下段配置
R2が後ろに下がったらトリガー押して配置し始める
![[Pasted image 20260703004319.png]]