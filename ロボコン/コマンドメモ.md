# SSH
sshで警告が出たとき
```
ssh-keygen -f '/home/sato/.ssh/known_hosts' -R '192.168.11.11'
```
# カメラ

```
# 設定確認
v4l2-ctl -d /dev/video2 --list-ctrls

v4l2-ctl --list-devices
```



カメラのキャリブレーション
```
ros2 run usb_cam usb_cam_node_exe --ros-args --params-file /workspaces/abu2026-ros2-ws/src/detection/ar_detector/config/camera/imx900_camera/usb_imx900.yaml 
```

```
ros2 run camera_calibration cameracalibrator --size 10x7 --square 0.024 --no-service-check --ros-args -r image:=/image_raw
```
